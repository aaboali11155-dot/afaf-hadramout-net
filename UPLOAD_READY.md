# عفاف حضرموت نت — نسخة الرفع النهائية

هذه نسخة الرفع التراكمية للمشروع بعد مراجعة المشاكل #1–#23.

## مهم
- لا تنشئ مشروع Supabase جديدًا.
- قاعدة Supabase الحالية هي القاعدة المرتبطة بالمشروع، وقد تم تطبيق إصلاحات قاعدة البيانات المطلوبة عليها.
- لا تشغّل `SUPABASE_SETUP.sql` على قاعدة البيانات الحالية؛ الملف مرفق كمرجع للمخطط فقط لأن قاعدة الإنتاج الحالية تحتوي على جداول وبيانات قائمة.
- لا توجد `node_modules` داخل النسخة.
- إعدادات Vercel يمكنها استخدام متغيرات البيئة `VITE_SUPABASE_URL` و`VITE_SUPABASE_ANON_KEY`.
