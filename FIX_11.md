# FIX 11 — تنسيق سجل المشرف بالعربية

## الهدف
تحسين طريقة عرض السجل فقط، بدون حذف أو تعديل البيانات الأصلية في قاعدة البيانات.

## التعديلات
- إضافة `src/lib/adminAuditLogFormatter.js`.
- ترجمة أكواد الإجراءات البرمجية الشائعة إلى عناوين عربية واضحة.
- تنسيق التاريخ والوقت بصيغة عربية سهلة القراءة.
- جلب أسماء المشرفين والمستخدمين من `profiles` بدل عرض UUID في الواجهة.
- ترتيب السجل من الأحدث إلى الأقدم.
- الاحتفاظ بالسجل الأصلي كما هو في قاعدة البيانات.
- تحويل التفاصيل المعروضة إلى وصف مقروء مع عدم تغيير القيمة الأصلية المخزنة.

## ملاحظة
إذا كان سجل قاعدة البيانات يستخدم أسماء أعمدة مختلفة لـ `admin_user_id` أو `target_user_id` أو `created_at`، يستخدم المساعد أكثر من اسم شائع، ولا يحذف أي بيانات.
