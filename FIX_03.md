# FIX 03 — ترتيب واجهة المحادثة والتمرير التلقائي

- تم تعديل `src/pages/MessagesPage.jsx` فقط.
- الرسائل أصبحت داخل منطقة تمرير مستقلة، مع حقل الكتابة وزر الإرسال مثبتين أسفل منطقة الرسائل داخل بطاقة المحادثة.
- أضيفت نقطة نهاية للمحادثة (`messagesEndRef`) للتمرير التلقائي إلى أحدث رسالة عند فتح المحادثة أو تغيّر عدد الرسائل.
- لم يتم تغيير الألوان أو الهوية البصرية أو تصميم الكروت.
- لم يتم تغيير قاعدة بيانات Supabase أو RLS.
