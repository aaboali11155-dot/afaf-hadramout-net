# FIX 04 — تجميع طلبات التواصل والرسائل في لوحة المشرف

تمت إضافة `src/lib/adminConversationGrouping.js` كمنطق تجميع معزول:

- مفتاح المحادثة يعتمد على زوج المستخدمين بدون اعتبار لاتجاه الإرسال.
- A→B وB→A يستخدمان نفس `pairKey`.
- طلبات التواصل من `contact_requests` والرسائل من `messages` تُدمج في سجل واحد.
- التفاعلات داخل السجل مرتبة من الأقدم إلى الأحدث.
- البطاقة الواحدة مرتبة حسب أحدث تفاعل.
- لا يوجد أي تغيير في CSS أو الألوان أو تصميم الكروت.

ملاحظة مهمة:
تم تجنب إعادة كتابة مكونات لوحة المشرف غير المعروفة بشكل تخميني. إذا كان المكون الحالي يستخدم أسماء مختلفة للبيانات، يكفي تمرير مصفوفتي `contactRequests` و`messages` إلى:
`groupAdminInteractionsByPair({ contactRequests, messages })`
ثم استخدام `groupedAdminConversations` كمصدر بطاقات المحادثات، مع استخدام `conversation.interactions` لتفاصيل التسلسل الزمني.
