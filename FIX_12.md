# FIX 12 — الرسائل غير المقروءة وترتيب المحادثات

- إضافة `src/services/messageReadService.js`.
- حساب عدد الرسائل غير المقروءة لكل محادثة اعتمادًا على `is_read`.
- الرسائل الواردة غير المقروءة فقط تُحسب.
- ترتيب المحادثات حسب أحدث رسالة تنازليًا.
- عند فتح محادثة، يتم تحديث رسائل الطرف الآخر إلى `is_read = true` ثم تصفير الشارة محليًا.
- لا يتم حذف الرسائل أو إنشاء جدول جديد.
- التصميم والألوان الحالية لم تتغير.

## متطلب قاعدة البيانات
يجب أن يحتوي جدول `messages` على عمود `is_read` من النوع boolean، وأن تسمح RLS للمستلم بتحديث رسائله الواردة فقط.
