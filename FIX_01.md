# إصلاح المشكلة رقم 1 — تكرار طلبات التواصل لنفس الحساب

## ما تم تغييره
- تمت إضافة `groupContactRequests()` في `src/services/contactRequestService.js`.
- يتم تجميع طلبات التواصل حسب `sender_user_id`/الحساب الآخر، مع استخدام `other_user_id` كمفتاح التجميع.
- الكرت المعروض يمثل أحدث طلب من نفس الحساب.
- يتم الاحتفاظ بكل الطلبات السابقة داخل `requests` و`request_ids` حتى يمكن عرض التاريخ كاملًا داخل المحادثة.
- تم تعديل `MessagesPage.jsx` لاستخدام التجميع قبل عرض الكروت.
- تم منع تكرار الكروت في الطلبات الواردة والمرسلة والمحادثات المعتمدة.
- عند فتح المحادثة، تُضم رسائل طلبات التواصل السابقة إلى سجل المحادثة بترتيب زمني.
- لم يتم تغيير أي CSS أو ألوان أو أبعاد أو تصميم بصري.

## ملاحظة
لم يتم تعديل قاعدة بيانات Supabase أو سياسات RLS في هذا الإصلاح.
