# FIX 02 — فصل طلبات التواصل عن الرسائل

- `MessagesPage.jsx`: لا يجلب أو يعرض `contact_requests` إطلاقًا؛ يعرض فقط الرسائل الفعلية المعتمدة من جدول `messages`، مجمعة حسب المستخدم الآخر.
- قبول طلب التواصل وحده لا ينشئ بطاقة محادثة؛ تظهر المحادثة فقط بعد وجود سجل رسالة فعلية.
- `ProfileDetailPage.jsx`: بعد وجود طلب تواصل معتمد بين الطرفين، نموذج الإرسال يضيف رسالة فعلية إلى `messages`. قبل الاعتماد يبقى النموذج لإنشاء طلب التواصل.
- لم يتم تغيير CSS أو الألوان أو أبعاد الواجهة.
- إشعارات طلب/قبول/رفض التواصل تعتمد على نظام `notifications` الموجود أصلًا في المشروع.
