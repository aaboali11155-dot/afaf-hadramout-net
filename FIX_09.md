# FIX 09 — إجراءات معالجة بلاغات المستخدمين

- استبدال أزرار «تم الحل/تجاهل» وحدها بقائمة إجراءات إلزامية داخل تفاصيل البلاغ.
- الإجراءات: تحذير، رسالة مباشرة، إخفاء الملف، تعليق، حظر، أو تجاهل البلاغ.
- لا يمكن إنهاء البلاغ المفتوح دون اختيار إجراء.
- الرسالة المباشرة تُرسل فعليًا إلى `messages` ثم يُغلق البلاغ ويُسجل الإجراء.
- إجراءات إخفاء/تعليق/حظر تغيّر `profiles.account_status`.
- كل إجراء يُسجل عبر `admin_audit_log` مع `report_id` و`target_user_id` داخل التفاصيل، إضافة إلى معرف المشرف والطابع الزمني.
- التصميم والألوان الحالية لم تُغيّر.

## ملاحظة RLS
إذا رفض Supabase أحد الإجراءات بسبب RLS، راجع سياسات `reports` و`profiles` و`messages` و`admin_audit_log` الحالية. لا يتم تجاوز RLS من الواجهة.
